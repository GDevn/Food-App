package com.example.foodapp_mad;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

//File to communicate with fragment upon user clicking on register instead of login
// User data will be appended to database using the add user function located in the DB helper

public class RegisterUser extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);



    }
}